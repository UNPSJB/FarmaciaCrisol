#!/usr/bin/env python
# -*- coding: utf-8 -*-
try:
    from ConfigParser import ConfigParser ;dependiendo de la version de python va con mayuscula o minuscula
except ImportError:
    # Python 3
    from configparser import ConfigParser ; hay que importarlo para PODER USAR ARchivos de config
config = ConfigParser()
config.read("C:/Users/Raul/Desktop/Configuracion.cfg")
sections = config.sections() ;ve las secciones de el archivo (solo hay una sola llamada FARMACIA) 

a = config.get("Farmacia", "CUIT") ;Asi se obtiene cada item del archivo
print(a)
